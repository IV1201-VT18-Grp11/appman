# https://www.playframework.com/documentation/latest/Configuration

play.evolutions.db.default.autocommit = false

slick.dbs.default {
    profile = "models.PgProfile$"
    db {
        driver = "org.postgresql.Driver"
        url = "jdbc:postgresql:///appman"
    }
}
